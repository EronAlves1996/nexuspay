services:
  postgres:
    image: postgres:18.3-alpine
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nexus_pay
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
